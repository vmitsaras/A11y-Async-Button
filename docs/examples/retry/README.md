# Retry Addon Example

This example demonstrates the optional `a11y-async-button/addons/retry` entry.

It shows:

- recoverable failed attempts
- retry text after non-final failures
- final error text after max attempts
- lifecycle events used to update visible status text

Run `npm run build` before opening the page so `docs/assets/addons/retry.js`
exists.
