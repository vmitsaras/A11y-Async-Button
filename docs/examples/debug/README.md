# Debug Addon Example

This example demonstrates the optional `a11y-async-button/addons/debug` entry.

It shows:

- structured diagnostics for async button markup
- form-addon setup checks
- error, warning, and info counts
- optional console logging

Run `npm run build` before opening the page so `docs/assets/addons/debug.js`
exists.
